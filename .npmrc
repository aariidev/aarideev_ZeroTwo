auto-install-peers=false
strict-peer-dependencies=false
# Don't fail when lifecycle scripts are ignored for packages outside onlyBuiltDependencies
strict-dep-builds=false
# Don't re-run install/deps gate before every `pnpm run` (was failing on IGNORED_BUILDS)
verify-deps-before-run=off
